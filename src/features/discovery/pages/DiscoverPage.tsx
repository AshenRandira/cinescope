import { RoutePlaceholder } from '../../../components/feedback/RoutePlaceholder'

export function DiscoverPage() {
  return (
    <RoutePlaceholder
      description="Discover will provide flexible browsing tools for genres, years, ratings, languages, providers, and other useful filters."
      eyebrow="Explore without limits"
      release="Release 1"
      title="Discover something unexpected."
    />
  )
}
