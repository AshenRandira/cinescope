import { RoutePlaceholder } from '../../../components/feedback/RoutePlaceholder'

export function LoginPage() {
  return (
    <RoutePlaceholder
      description="Secure Firebase Authentication will be connected here during the user-platform release."
      eyebrow="Welcome back"
      release="Release 2"
      title="Log in to CineScope."
    />
  )
}
